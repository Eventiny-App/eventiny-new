{
  "name": "eventiny-new",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @eventiny/web",
    "build": "npm run build --workspaces",
    "typecheck": "tsc -b",
    "lint": "echo \"No linter configured yet\""
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
